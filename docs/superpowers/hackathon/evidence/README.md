# Deployment evidence (submission)

Drop Qwen Cloud console screenshots here before submitting:
- `qwen-usage-dashboard.png` — API usage / request log showing live inference
- `qwen-account.png` — account / API keys page
- `qwen-console-surface.png` — any Workbench / Apps / Deployments surface if the account exposes one

These, plus the code links in the README's "Alibaba Cloud deployment proof" section, are the submission's deployment evidence.
