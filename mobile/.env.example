# Copy to .env.local and fill in. EXPO_PUBLIC_* vars are read at bundle time
# (restart Metro after changing). Used as the default OpenRouter chat key.
EXPO_PUBLIC_OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
